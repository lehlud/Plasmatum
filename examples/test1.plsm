fdef fak on (i) -> i > 1 ? i * fak on (i-1) : i

fdef ifak on (i) -> (
    def result = 1
    while i > 1 ? (
        set result = result * i
        set i = i - 1
    )
) -> result

echo ifak on (1000)
